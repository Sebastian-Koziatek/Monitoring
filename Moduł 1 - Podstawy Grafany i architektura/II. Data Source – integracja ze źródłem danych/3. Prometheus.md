# Prometheus

## Wprowadzenie do Prometheusa

![Prometheus](/grafiki/m1-015-prometheus.png)

**Prometheus** to otwartoźródłowe narzędzie do monitorowania i alertowania, zaprojektowane z myślą o elastycznym zbieraniu metryk z systemów oraz aplikacji. Działa jako baza danych szeregów czasowych (time-series database) z wbudowanym mechanizmem zapytań i obsługą alertów.

### Kluczowe cechy Prometheusa:

- Gromadzenie danych za pomocą mechanizmu pull (odpytywanie endpointów HTTP).
- Wbudowany język zapytań **PromQL** do analizy i wizualizacji danych.
- Niezależność od zewnętrznych baz danych, dzięki lokalnemu przechowywaniu danych.

### Infrastruktura

![Prometheus](/grafiki/m1-022-prometheus.png)

```
┌───────────────────────────────────────────────────┐
│                Monitorowane hosty                 │
│                                                   │
│  ┌─────────────┐  ┌─────────────┐  ┌───────────┐  │
│  │    Node     │  │   cAdvisor  │  │    App    │  │
│  │  Exporter   │  │  (Docker)   │  │  /metrics │  │
│  │   :9100     │  │   :8080     │  │   :9xxx   │  │
│  └─────────────┘  └─────────────┘  └───────────┘  │
└───────────────────────────────────────────────────┘
              │               │              │
              └───────────────┴──────────────┘
                              │  HTTP scrape (pull)
                              ▼
               ┌──────────────────────────┐
               │    Prometheus Server     │
               │    :9060                 │
               │   ┌────────────────────┐ │
               │   │  Time-series       │ │
               │   │  storage           │ │
               │   └────────────────────┘ │
               └─────────────┬────────────┘
                             │  PromQL / HTTP
                             ▼
               ┌──────────────────────────┐
               │         Grafana          │
               └──────────────────────────┘
```

## Grafana i Prometheus

Grafana jest świetnym narzędziem do wizualizacji danych zgromadzonych w Prometheusie. Integracja tych narzędzi pozwala na tworzenie zaawansowanych dashboardów, łatwe monitorowanie systemów oraz ustalanie progów alertów.

## Instalacja i konfiguracja Prometheusa

### Instalacja Prometheusa (na przykładzie Ubuntu 24.04)

> Zawsze sprawdzaj aktualną wersję na stronie [GitHub Releases](https://github.com/prometheus/prometheus/releases) — podana niżej wersja może być nieaktualna.

1. Pobierz i rozpakuj Prometheusa:
   ```bash
   wget https://github.com/prometheus/prometheus/releases/download/v2.47.0/prometheus-2.47.0.linux-amd64.tar.gz
   tar xvf prometheus-2.47.0.linux-amd64.tar.gz
   cd prometheus-2.47.0.linux-amd64
   ```
2. Skopiuj pliki binarne i konfigurację:
   ```bash
   sudo mv prometheus promtool /usr/local/bin/
   sudo mkdir /etc/prometheus /var/lib/prometheus
   sudo mv prometheus.yml /etc/prometheus/
   ```
3. Utwórz dedykowanego użytkownika systemowego:
   ```bash
   sudo useradd --no-create-home --shell /bin/false prometheus
   sudo chown -R prometheus:prometheus /etc/prometheus /var/lib/prometheus
   ```
4. Utwórz plik usługi systemd:
   ```bash
   sudo nano /etc/systemd/system/prometheus.service
   ```
   Wklej zawartość:
   ```ini
   [Unit]
   Description=Prometheus
   Wants=network-online.target
   After=network-online.target

   [Service]
   User=prometheus
   Group=prometheus
   Type=simple
   ExecStart=/usr/local/bin/prometheus \
     --config.file=/etc/prometheus/prometheus.yml \
     --storage.tsdb.path=/var/lib/prometheus/ \
     --web.listen-address=:9060

   [Install]
   WantedBy=multi-user.target
   ```
5. Uruchom i włącz usługę:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable prometheus
   sudo systemctl start prometheus
   sudo systemctl status prometheus
   ```

### Przykładowa konfiguracja `prometheus.yml`

```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets: ['localhost:9100']
```

- **scrape_interval:** Określa częstotliwość zbierania danych.
- **job_name:** Unikalna nazwa zadania zbierania danych.
- **targets:** Lista endpointów do monitorowania (w tym przypadku Node Exporter).

## Instalacja Node Exporter

**Node Exporter** to agent Prometheusa zbierający metryki systemu operacyjnego Linux: CPU, pamięć, dysk, sieć i inne. Jest wymagany do uzyskania danych z serwerów opisanych w przykładach PromQL poniżej.

> Sprawdź aktualną wersję na [GitHub Releases](https://github.com/prometheus/node_exporter/releases).

1. Pobierz i zainstaluj Node Exporter:
   ```bash
   wget https://github.com/prometheus/node_exporter/releases/download/v1.7.0/node_exporter-1.7.0.linux-amd64.tar.gz
   tar xvf node_exporter-1.7.0.linux-amd64.tar.gz
   sudo mv node_exporter-1.7.0.linux-amd64/node_exporter /usr/local/bin/
   sudo useradd --no-create-home --shell /bin/false node_exporter
   ```
2. Utwórz plik usługi systemd:
   ```bash
   sudo nano /etc/systemd/system/node_exporter.service
   ```
   Wklej zawartość:
   ```ini
   [Unit]
   Description=Node Exporter
   Wants=network-online.target
   After=network-online.target

   [Service]
   User=node_exporter
   Group=node_exporter
   Type=simple
   ExecStart=/usr/local/bin/node_exporter

   [Install]
   WantedBy=multi-user.target
   ```
3. Uruchom usługę:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable node_exporter
   sudo systemctl start node_exporter
   ```
4. Dodaj Node Exporter jako target w `prometheus.yml` i przeładuj Prometheusa:
   ```bash
   sudo systemctl reload prometheus
   ```

## Konfiguracja Grafany z Prometheusem

1. Zaloguj się do interfejsu webowego Grafany.
2. Przejdź do sekcji **Connections > Data Sources**.
3. Kliknij przycisk **Add data source** i wybierz **Prometheus**.
4. Wypełnij pola:
   - **URL**: `http://localhost:9060`
   - Pozostaw pozostałe ustawienia domyślne.
5. Kliknij **Save & Test**, aby upewnić się, że Grafana poprawnie połączyła się z Prometheusem.

## Tworzenie Dashboardów

### Tworzenie panelu z wykorzystaniem PromQL

1. Przejdź do sekcji **Dashboards > New Dashboard** w Grafanie.
2. Dodaj nowy panel (**Add a new panel**).
3. Wybierz Prometheus jako źródło danych.
4. Wpisz zapytanie PromQL w polu **Metrics browser** (przykłady poniżej).
5. Dostosuj wizualizację (np. wykres liniowy).
6. Zapisz panel i dodaj go do dashboardu.

### Przykładowe zapytania PromQL

#### 1. Średnie wykorzystanie CPU (wszystkie rdzenie)
```PromQL
100 - (avg(rate(node_cpu_seconds_total{mode="idle"}[15m])) * 100)
```

**Wyjaśnienie zapytania:**
- `node_cpu_seconds_total{mode="idle"}` - metryka Node Exporter pokazująca czas spędzony przez CPU w trybie bezczynności
- `rate(...[15m])` - oblicza średnią zmianę wartości na sekundę w ciągu ostatnich 15 minut
- `avg(...)` - oblicza średnią ze wszystkich rdzeni CPU
- `100 - (... * 100)` - konwertuje bezczynność na wykorzystanie CPU w procentach (100% - bezczynność = wykorzystanie)

#### 2. Trend wykorzystania pamięci (średnia z 15 minut)
```PromQL
avg_over_time(((node_memory_MemTotal_bytes - node_memory_MemAvailable_bytes) / node_memory_MemTotal_bytes * 100)[15m:1m])
```

**Wyjaśnienie zapytania:**
- `node_memory_MemTotal_bytes` - całkowita ilość pamięci RAM w systemie (w bajtach)
- `node_memory_MemAvailable_bytes` - ilość dostępnej pamięci RAM (w bajtach)
- `node_memory_MemTotal_bytes - node_memory_MemAvailable_bytes` - oblicza użytą pamięć
- `(...) / node_memory_MemTotal_bytes * 100` - konwertuje na procent wykorzystania pamięci
- `avg_over_time(...[15m:1m])` - oblicza średnią wartość w oknie 15 minut z próbkowaniem co 1 minutę

## Alerting: Prometheus Alertmanager vs Grafana Alerting

Prometheus posiada własny **Alertmanager** do wysyłania powiadomień, niezależny od Grafana Alerting. Oba systemy mogą działać równolegle:

| | **Prometheus Alertmanager** | **Grafana Alerting** |
|---|---|---|
| Reguły alertów | Definiowane w plikach YAML (PromQL) | Definiowane w UI Grafany |
| Źródła danych | Tylko Prometheus | Dowolne źródło danych Grafany |
| Kanały powiadomień | Email, Slack, PagerDuty, Webhook | Email, Slack, PagerDuty, OnCall i inne |
| Zarządzanie | Pliki konfiguracyjne | UI Grafany |
| Zalecenie | Alerty infrastrukturalne niezależne od Grafany | Alerty oparte na wielu źródłach danych |

## Najlepsze praktyki

- **Optymalizacja:** Używaj odpowiednio dobranych przedziałów czasowych, aby uniknąć nadmiernego obciążenia Prometheusa.
- **Eksploracja danych:** Wykorzystuj wbudowany interfejs Prometheusa (`http://localhost:9060`) do testowania zapytań PromQL przed ich użyciem w Grafanie.
- **Alerting:** Zdecyduj, czy alerty mają być zarządzane przez Alertmanager (infrastruktura) czy Grafana Alerting (dashboardy i wiele źródeł).

## Podsumowanie

Prometheus i Grafana są fundamentem nowoczesnych systemów monitoringu. Łatwość integracji oraz możliwość tworzenia zaawansowanych zapytań PromQL pozwala na pełne wykorzystanie potencjału danych metrycznych. Node Exporter dostarcza gotowych metryk dla serwerów Linux, a Alertmanager lub Grafana Alerting zapewniają system powiadomień. Przy odpowiedniej konfiguracji narzędzia te mogą dostarczyć kluczowych informacji o stanie infrastruktury IT.
